version: 2

models:
  - name: dim_customers
    description: "Consolidated customer dimension table."
    columns:
      - name: account_id
        tests:
          - unique
          - not_null
      - name: customer_status
        tests:
          - not_null
